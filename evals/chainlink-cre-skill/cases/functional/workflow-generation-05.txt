I need you to write a CRE TypeScript workflow that reads state for a configurable list of onchain resources, compares each item against per-resource configuration, and submits an onchain action when an item needs service. Include any minimal support contract or interface needed for the onchain action path, the workflow config, and a simulation command. Avoid leaving unexplained external dependencies.
