I need a CRE TypeScript workflow that compares a value read from an onchain contract with a value fetched from an external API on a fixed schedule. When the configurable deviation threshold is crossed, it should send a notification through a configured endpoint. Generate the workflow and config files. Make sure all generated files use explicit units and preserve the same schedule, threshold, chain, and secret semantics across environments.
